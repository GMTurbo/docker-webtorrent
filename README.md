# Docker-Webtorrent
## for distributed torrent seeding

## to run
git clone this repo
```bash
cd <repo>
chmod 755 ./download
./download [it, d] <magnetlink>
#it = iterative mode
#d = daemon mode
```

d  = daemon mode

A handly command
```bash
alias dl="bash {path_to_repo}/download"
```